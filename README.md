My personal web, published at [gmunguia.com](https://gmunguia.com).

Deployment:
* set up aws credentials with `aws configure --profile gmunguia_website`
* build with `npm run build`
* deploy with `npm run deploy`
